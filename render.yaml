services:
  - type: web
    name: narraplay
    runtime: node
    buildCommand: cd frontend && npm install && npm run build
    startCommand: npx serve -s frontend/dist -l 10000
    envVars:
      - key: NODE_VERSION
        value: 18